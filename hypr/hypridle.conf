# Auto-generated by hypridle-manager.sh based on power profile: performance
# Original backed up at: /home/l0n3/.config/hypr/hypridle.base.conf
# Last updated: 2025-11-01 17:50:27

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

# Screen lock
listener {
    timeout = 300
    on-timeout = loginctl lock-session
}

# Suspend disabled for this profile
# listener { ... }
